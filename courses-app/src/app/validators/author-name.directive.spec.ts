import { AuthorNameDirective } from './author-name.directive';

describe('AuthorNameDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorNameDirective();
    expect(directive).toBeTruthy();
  });
});
